    
namespace TonysRedis
{
    using System.Net;
    using System.Net.NetworkInformation;
    using System.Net.Sockets;
    using System.Text;

    internal class Program
    {
        static void Main(string[] args)
        {
            //TcpListener svr = new TcpListener(IPAddress.Any,port: 6969);
            //svr.Start();
            ////svr.AcceptSocket(); // 1.3
            //var client = svr.AcceptSocket(); // client is 'socket' == conn (eg browser or terminal)
            //WriteLine("sending back: '+PONG'");
            //var bytes_response = Encoding.ASCII.GetBytes("+PONG\r\n");
            //client.Send(bytes_response);

            ////client.Send(response.)
        }
    }
}
